# Repository Instructions

## Git Workflow

In this repository, "push" means commit to `main` and push directly to `origin/main`.

Do not create feature branches or pull requests for normal changes. Use a branch or PR only when the user explicitly asks for one.
